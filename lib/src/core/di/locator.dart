import 'package:get_it/get_it.dart';

GetIt locator = GetIt.instance;

Future<void> setupServices() async {
  /// register providers
  // locator.registerLazySingleton<MoodProvider>(() => MoodProvider());
}
